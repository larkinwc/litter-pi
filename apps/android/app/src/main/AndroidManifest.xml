<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Litter"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting">
        <meta-data
            android:name="com.litter.android.runtime.STARTUP_MODE"
            android:value="${runtimeStartupMode}" />
        <meta-data
            android:name="com.litter.android.runtime.ENABLE_ON_DEVICE_BRIDGE"
            android:value="${enableOnDeviceBridge}" />
        <activity
            android:name="com.litter.android.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
              Anthropic OAuth (Claude Code) PKCE redirect. The Chrome
              Custom Tab launched from `AnthropicOAuthScreen` redirects
              back to `litter://oauth/pi/anthropic?code=...&state=...`
              after the user finishes the consent screen. The deep-link
              intent filter funnels the redirect into `MainActivity`
              which forwards the URI to `AnthropicOAuthCallbackBus`
              for the Compose surface to consume. See VAL-AUTH-003.
            -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="litter"
                    android:host="oauth"
                    android:pathPrefix="/pi/anthropic" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.litter.android.auth.ChatGPTOAuthActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="com.litter.android.auth.ChatGPTOAuthRedirectActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="litterauth"
                    android:host="chatgpt-auth-complete" />
            </intent-filter>
        </activity>

        <service
            android:name="com.litter.android.state.PetOverlayService"
            android:exported="false" />
        <service
            android:name="com.litter.android.push.LitterFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <receiver
            android:name="com.litter.android.ui.widget.ActiveTurnWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/active_turn_widget_info" />
        </receiver>
    </application>

</manifest>
